import React from "react";
import Layout from "../components/Layout";

const Dashboard = () => {
    return (
        <Layout>
            <h2>Dashboard</h2>
            <p>Aqui você verá os preços das criptomoedas em tempo real.</p>
        </Layout>
    );
};

export default Dashboard;
